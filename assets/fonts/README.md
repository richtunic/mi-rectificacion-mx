# Fuente incorporada

`Roboto-Medium.ttf` se distribuye como parte del material de ejemplo de `printpdf 0.8.2` y se incorpora para renderizar correctamente texto Unicode en los escritos PDF multiplataforma.
